import { VocalesPorNumerosPipe } from './vocales-por-numeros.pipe';

describe('VocalesPorNumerosPipe', () => {
  it('create an instance', () => {
    const pipe = new VocalesPorNumerosPipe();
    expect(pipe).toBeTruthy();
  });
});
